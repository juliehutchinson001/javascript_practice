class Dom {
    hideElement(id) {
        // logic to hide from dom
    }

    showElement(id) {
        // logic to show in dom
    }

    updateElement(name, id) {
        // logic to update in dom
    }

}

const dom = new Dom;

export default dom;
